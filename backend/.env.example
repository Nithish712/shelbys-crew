# Supabase — Replace with your Supabase project credentials
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# JWT Secret (keep this secret!)
JWT_SECRET=shelby_crew_secret_peaky_blinders_2025

# Admin Login
ADMIN_USERNAME=admin
# Run: node -e "require('bcryptjs').hash('your_password',10).then(console.log)" to generate
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here

# CORS — add your deployed frontend URLs (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

PORT=5000
